To send a local file through the relay, put this directive on its own line when needed:

ATTACH ./artifacts/chart.png

Relative paths resolve against the current workdir.
Use one ATTACH line per file.
